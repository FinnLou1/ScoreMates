specVersion: "v1"
serviceId: "scoremates"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "scoremates-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
